# Prefect Webhooks can be imported via name in the form `name/my-bot-name`
terraform import prefect_webhook.example name/my-webhook-name

# Prefect Webhooks can also be imported via UUID
terraform import prefect_webhook.example 00000000-0000-0000-0000-000000000000
